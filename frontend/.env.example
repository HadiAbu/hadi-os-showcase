# Copy to .env. In Docker the app is served behind nginx, so /api is correct.
# For `npm run dev` outside Docker, point at the backend directly, e.g.
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=/api
